/**
 * Current version of `kf-cli`.
 */
// eslint-disable-next-line @typescript-eslint/no-unsafe-member-access, @typescript-eslint/no-var-requires
export const version = require('../../package.json').version as string
